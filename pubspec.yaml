name: auryel
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+7

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.2
  phosphor_icons: ^3.0.1
  share_plus: ^13.3.0
  audioplayers: ^6.8.1
  shared_preferences: ^2.5.5
  path_provider: ^2.1.6
  http: ^1.6.0
  flutter_secure_storage: ^10.0.0
  in_app_purchase: ^3.3.0
  video_player: ^2.14.0
  # Sortie vers la gestion d'abonnement Google Play (résiliation / modification).
  # Aucun paiement web, aucun steering — uniquement l'URL OFFICIELLE du store.
  url_launcher: ^6.3.1
  # « Noter l'appli » — boîte de notation NATIVE du store (Play In-App Review /
  # StoreKit). Déclenché UNIQUEMENT par un tap explicite de l'utilisateur,
  # jamais de pop-up automatique, aucun review gating, aucune récompense.
  in_app_review: ^2.0.10
  google_mobile_ads: ^9.1.0
  # Push Android (FCM). L'app démarre SANS Firebase configuré : sans
  # google-services.json + plugin Google Services, `Firebase.initializeApp()`
  # échoue proprement et le service passe en état « indisponible » (aucun crash,
  # tests OK). Le plugin gradle `com.google.gms.google-services` N'EST PAS
  # appliqué (il casserait le build sans google-services.json) — cf. rapport.
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0
  # Affichage d'une notification locale quand un message arrive app au premier
  # plan (Android n'affiche rien tout seul dans ce cas). Sert aussi à créer le
  # canal `auryel_default` (visibilité PRIVATE, rien de sensible verrouillé).
  flutter_local_notifications: ^19.4.2
  # « Ouvrir les réglages » (ré-autoriser les notifications après un refus).
  permission_handler: ^12.0.1
  # Meta App Events — mesure d'installation / conversion, UNIQUEMENT après
  # consentement explicite. Aucune donnée personnelle transmise. Enveloppé par
  # la façade lib/analytics/meta_events.dart : aucun écran n'appelle ce package.
  facebook_app_events: ^0.20.1
  saga_map: ^2.0.0
  # Réveil Auryel — voix du message matinal en repli LOCAL quand aucun MP3
  # pré-généré (R2) n'existe encore pour le message tiré (jamais un appel TTS
  # payant/réseau au moment où l'alarme sonne). Voir lib/screens/wake_ringing_screen.dart.
  flutter_tts: ^4.2.5
  # Lecteur PDF intégré pour les ebooks Bien-être (aucun navigateur externe).
  pdfrx: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/conseillers/
    - assets/vocaux/
    - assets/videos/
    - assets/images/tarot/
    - assets/images/tarot_card_back.png
    - assets/images/tarot_table_blank.png
    - assets/wake_sounds/
    - assets/images/wake/
    - assets/wake_motivations/
    - assets/pensees/pensees_100_site.json
    - assets/pensees/publications/

  # Typographie Auryel — Inter, EMBARQUÉE (aucun chargement réseau au runtime).
  # Fichiers : rsms/inter v4.1 · SIL Open Font License 1.1 (usage commercial /
  # applicatif autorisé). Licence complète dans fonts/Inter/OFL.txt.
  # Graisses volontairement limitées à 400 / 500 / 600 / 700.
  fonts:
    - family: Inter
      fonts:
        - asset: fonts/Inter/Inter-Regular.ttf
          weight: 400
        - asset: fonts/Inter/Inter-Medium.ttf
          weight: 500
        - asset: fonts/Inter/Inter-SemiBold.ttf
          weight: 600
        - asset: fonts/Inter/Inter-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
